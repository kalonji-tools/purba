---
name: Defect
about: A task to do, because a claim in the tree is false
labels: "wayfinder:task"
---

## Task

<!-- What must become true, and the file that carries the false claim.

     ⚠️ Your reader has to reproduce this before they will act on it. Write
     for somebody who does not yet believe you.

     Example. One issue runs through every section of this template.

       **Correct the command the README names for running one test.**
       File: `README.md` -->

**Done when**
<!-- An observable event.

     Example, continued.

       **Done when** the README names a command that runs, and running it
       exits zero. -->

## The claim that is false

<!-- Quote the span verbatim and name the file. Quote it; a paraphrase moves
     the argument onto your wording instead of the tree's.

     Example, continued.

       `README.md` says:

       > Run a single test with `purba run <name>`. -->

## What it actually does

<!-- A runnable command and its output. Cite `main` at a SHA, never a version,
     while there is no release to name.

     Example, continued.

       Against `main` at `89c9fcc`:

       ```
       $ purba run smoke
       error: unrecognized subcommand 'run'
       ```  -->

## Where it came from

<!-- The review, run or reading that surfaced it. ⚠️ A defect that cannot say
     where it came from is usually two defects wearing one title.

     Example, continued.

       Found while reading the README to write the install steps. -->
