---
name: Task
about: A task to do, rather than a question to settle
labels: "wayfinder:task"
---

## Task

<!-- The imperative, in bold, and the file paths it writes.

     Your reader picks this up cold, and possibly months from now. Name the
     files so they do not have to search for the work.

     Example. One issue runs through every section of this template.

       **Write the licence gate.** File: `deny.toml` -->

**Done when**
<!-- An observable event, proven by execution rather than by reading a setting.
     ⚠️ A check that runs and does not block is a suggestion, so name the
     failure rather than the feature.

     Example, continued.

       **Done when** `cargo deny check licenses` exits non-zero on a
       dependency whose licence is not on the allowlist. -->

## Out of scope

<!-- What is already settled, so nobody reopens it. Short, negative, and last.

     Example, continued.

       - Which licences are allowed. The allowlist is generated from the
         lockfile, never authored, so no row here is chosen by hand. -->
